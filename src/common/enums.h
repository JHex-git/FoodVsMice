#pragma once
enum class FoodType
{
    HOTPOT = 0,
    CHERRY,
    EGG
};

// TODO: 之后有新的老鼠
enum class MouseType
{
    NORMAL_MOUSE=0,
    BUCKET_MOUSE,
    HELMET_MOUSE
};

enum class MapType
{
    BASE = 0
};
